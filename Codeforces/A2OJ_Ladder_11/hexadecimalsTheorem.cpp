//TIME : 10min
//어이가 없게도 쉬운방법이 존재하는 넌센스 문제가 있다.
#include <bits/stdc++.h>  
using namespace std;
 
typedef long long ll;
typedef unsigned long long ull;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL) ; cout.tie(NULL) ;
    cin.tie(NULL) ; cout.tie(NULL) ;
    ll n;
    cin >> n;
    cout << 0 << " " << 0 << " " << n << endl;
}